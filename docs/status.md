# Roastnode Current Status

Last reviewed: 2026-08-29

This is the compact public status ledger for humans and AI agents. It distills the original product context plus the slice docs in this repository. Update it whenever a slice changes what is done or intentionally deferred.

## Built Now

- Rails 8.1 monolith at the repository root, running Ruby 3.3.12 and PostgreSQL 17.11, with Hotwire, Turbo, Tailwind CSS, Active Storage, Solid Queue, Docker Compose, and local defaults for web port `3001` and PostgreSQL host port `5433`.
- Dependency maintenance was refreshed on 2026-08-20: Rails is at 8.1.3.1, every gem allowed by the existing compatibility constraints is current, importmap packages are current, and fresh RubySec, importmap, and Brakeman scans report no vulnerabilities or warnings. The audit record documents the constrained `bindata` 3.x and intentionally deferred cdxgen 13.x major upgrades.
- Rails-native authentication, first-user setup for empty installs, password reset, signed-in password change flows, optional user passkeys with browser-picker login and passkey second factor, private-by-default app shell, and an instance admin dashboard with safe read-only checks plus backup controls.
- Workspace core: household onboarding, active workspace switching, owner/admin/member/viewer roles, invite links with optional email delivery and optional username capture during invite signup, accepted invite status display, resend/re-invite for email-bound invites, private invite-only account creation, instance-admin email invites for creating separate new households while public registration remains disabled, member role management/removal, owner-only ownership transfer, owner-only workspace deletion, and workspace-scoped controller patterns.
- Profile settings: display name, username-style display label, avatar, public banner, preferred landing screen, enabled brew methods, grams per coffee spoon for Quick Drip estimates, espresso focus field, hidden espresso fields, number format, time format, and per-user IANA timezone.
- Workspace settings: household name, currency, logo, banner, configurable Buy Me a Coffee support badge mode for public shared pages, public brew-share management with URLs, linked brews, edit/remove actions, view counts, and retained recent viewer IP history, and public bean-share management with URLs, linked beans, enabled/protected state, view counts, and retained recent viewer IP history.
- Coffee core: rich beans, equipment, espresso brews, Quick Drip brews for private automatic drip/filter-style daily logging, inventory adjustments, retention markers, dashboard activity, required-open-bean logging, and Workspace-safe Served-to choices for Self, another current household member, or a private Guest name with an independent Cup style.
- Cupping requests: saved Espresso sharing sends another current household member the authenticated private Brew URL and gives a Guest a separate unlisted snapshot page. First access starts one 24-hour window for authoritative taste/rating updates and a private comment, with English/German browser localization, request-specific safe identity media, rate limiting, append-only Guest/IP audit events, idempotent Solid Queue expiry plus recurring recovery, serving-change revocation, workspace JSON comment-only portability, and full instance-backup restoration.
- Activity audit: durable append-only ActivityEvent ledger for successful authenticated mutations and system operations, with category/actor/date filters, workspace and administrative visibility boundaries, safe metadata snapshots, deletion tombstones, and workspace-export/instance-backup preservation.
- Beans: rich metadata, richer origin/manufacturer fields, variety information, search-while-type roaster suggestions from existing workspace bean history, additive photos, primary photo, crop/download/remove media controls, derived bag statuses (`stock`, `open`, `finished`, transitional `used_up`, `archived`), grouped overview sections for open, stock, finished/used-up, and archived bags, active-bag sorting by newest brew use, stock-aware opened-date handling, one-way bag-size-to-remaining sync for stock bags, normal finished-bag lifecycle with `finished_at` and used/days/grams-per-day stats, exceptional issue/archive card, quick-open and Rebuy actions, duplicate-as-new-bag, duplicate-label disambiguation, manual inventory adjustment UI, compact overview/detail remaining cards with rating and color-coded remaining bars, cost metrics, curated public bean sharing for publishable opened/finished/used-up/previously-opened archived bags, and danger-zone delete with dependent brew/inventory cleanup. New and duplicated bags begin as full unopened stock. Bean entry has readable 1–5 rating controls, exact field examples, and separate validated private Purchase Website and Coffee Origin Website fields rendered as synthesized private link chips. Bean detail supports focused rating correction and links Recent brews into a workspace-safe Bean-filtered Coffees history that preserves its scope across Compact/Hero views, compatible type filters, and pagination. Cost per shot is explicitly Espresso-only: purchase price per gram multiplied by average Bean In, with an 18g fallback before the first Espresso.
- Bean grinder tendencies: bean detail pages always show a grinder tendency card. After a non-duplicated bag has its first brew, the app can suggest a same-grinder adjustment from that first brew's setting, ratio, and total time toward the current espresso target of `1:2.5` in `25-30s`. Suggestions use workspace brew history as comparable context, safe auto-detection for Eureka-style `turn/dial` strings and plain numeric settings, and small relative moves instead of jumping to one absolute historical setting. Blank, inconsistent, or unknown setting strings are skipped instead of raising errors. The card also shows per-bean grinder-setting distribution. New duplicated bags record their source bag, automatic suggestions are suppressed for those bags, and a manual in-card calculation remains available.
- Espresso logging: curated last-brew defaults for bean, grinder, machine, active preparation tools, grind setting, brew temperature, and machine-supported pre-infusion/low-flow-start seconds, using the user's last active-workspace brew or the workspace's last brew for users without household brew history. When an Espresso bean switch exposes a differing saved best-reference Grind setting, a compact field action can copy that exact text explicitly without changing the selected grinder; matching or unavailable settings show no action. Machine settings determine whether new logs expose pre-infusion, low-flow-start, and flow-control-used inputs while edits preserve historical values. Bean weights, dose, yield, total time, first drip, rating, channeling, flow-control use, notes, taste, serving metadata, and photos stay fresh during normal logging. Repeat Good Brew links from private brew details and dashboard cockpit best brews prefill targetable shot/setup values from a selected source brew, use an open duplicated follow-up bag when the source bean is closed, and keep taste, notes, media, serving metadata, and sharing fields fresh. Ground out can prefill dose one way while logging.
- Quick Drip logging: method tabs on the Log screen, profile-enabled methods, brewer equipment, machine cups, coffee spoons or measured ground coffee, spoon-based inventory estimates using the user's grams-per-spoon preference with a 5g fallback, pre-ground bean support, method-scoped preparation tools, Weak/Balanced/Harsh taste labels, and method-specific private Hero Cards without espresso chart/retention. Brew Log bean choices now show grams and percentage remaining, identify the operator's method-specific last-used bean, and reveal a compact grinder-check reminder when another bean's best saved reference differs or is unavailable.
- External Coffee logging: separate private workspace records for purchased/out-of-home cafe drinks that do not consume bean inventory or require household equipment. External Coffees have a default Log screen tab, dedicated index/detail/edit routes, a broad searchable drink-type catalogue plus workspace-history suggestions, workspace-history place suggestions, optional drink size/place/location capture/price, Brew-style taste/rating controls, private note, public note, private photos, public/private links, photo-forward Hero Cards with workspace/user identity images when present, dashboard/activity participation, mixed Coffees history at `/coffees` with All/Brews/External filters, workspace JSON and CSV export, media ZIP export, and instance backup/restore coverage. Public External Coffee sharing is deferred.
- Recipe profiles: workspace-private espresso recipes can be created from existing brews, preserve public-safe source brew snapshots, expose editable exact targets, structured finish ingredients, finish notes, and one optional private finished-drink photo, and show prominent target markers for grinder, dose, timing, yield, temperature, and watch notes. Creating from a source brew can explicitly reuse the brew's primary photo without auto-selecting it. Recipe-guided logging keeps normal brew defaults unchanged, stores `recipe_id` plus a brew-time `recipe_snapshot`, and renders a subtle Hero Brew Card target ghost. Portable recipe JSON export/import creates unlinked private snapshots with finish ingredients and finish notes, without auto-creating beans, equipment, tools, brews, or media. Public recipe sharing uses unlisted, optionally password-protected token pages with curated snapshots, public finish details, public source notes, public recipe links, and explicitly selected recipe photos served through opaque media handles. Viewers can read recipes but cannot create, edit, delete, export, import, share, or start recipe-guided logging.
- Brew corrections: edit and delete flows keep bean inventory consistent through correction/reversal helpers, while saved taste balance/rating and the shared Served-to/Cup control have focused transaction-safe correction paths. Recipient roster tokens are resolved only through the active Workspace, and serving corrections cannot change inventory or unrelated Brew data.
- Hero Brew Card: dense screenshot-friendly Brew cards on dashboard, history, and details, with safe `Logged by … for …` identity, distinct icon/text/color recipient badges, paired current-member avatars, and an independent Cup badge. The upper section has a decorative two-half backdrop: a no-crop contained Bean primary on black at left and a covered Brew primary on black at right, with readable wash/gradients and no cross-half substitution. The Espresso chart and Quick Drip footer stay outside that upper section. Espresso cards include ratio, rating, balance, retention, SVG brew chart, first drip, pre-infusion, total time, and vertical temperature label; Quick Drip cards show batch metrics and mark estimated consumed grams with `~`.
- Brew detail pages: intentional saved-brew landing screens after logging, with a visible mobile-friendly heading, full log details below the card, private serving metadata, related bean/equipment/preparation-tool photos, and cross-links.
- Public brew sharing: workspace writers can publish their own espresso brews through curated, optionally password-protected token pages with a snapshot-driven Hero Brew Card that mirrors the private graph treatment without recipe ghosts. Its automatic recipient projection keeps Guests generic, keeps former members label-only, and permits a household-recipient avatar only with current membership. Selected current Bean/Brew primaries can fill the two Hero halves only when each attachment is also selected on that share; all media stays behind opaque handles, live allowlists, password gates, and safe-raster delivery. Public notes, selected photos, visible public links, prominent buy/affiliate links, bean public purchase/opening details, the optional workspace support badge, snapshot refreshes, native mobile share shortcuts, management permissions, and capped view history remain supported.
- Public bean sharing: workspace writers can publish their own opened, finished, used-up, or previously opened archived bean bags through curated, optionally password-protected token pages with snapshot-driven inventory, brew-count, consumption, rating, channeling, taste, grinder-setting, open-duration, and compact clustered timeline summaries. Every Espresso and Quick Drip summary and journey marker reuses the same automatic public recipient projection: Self, generic Guest, current household label/avatar, former-member label without avatar, or identity-free malformed fallback. No Guest name or Cup enters public snapshots. Privacy-safe workspace comparison badges, selected bean package photos, named public Brew chips, support badge, full-width details flow, targeted refreshes, management permissions, opaque live-authorized media, and capped view history remain supported.
- Preparation tools: espresso and Quick Drip method-scoped checklist records with ordering, owner/admin-only lifecycle and media management, member-visible tool use in brew logging, brew snapshots, and detail analytics.
- Equipment: grinder, machine, and brewer records with owner/admin-only edit/archive/reopen/delete lifecycle and media management, configurable machine extraction capabilities, member-visible gear details, list photos, detail analytics, and historical brew safety when equipment is deleted.
- Equipment events: first-class maintenance logs with minute-precision required timestamps, grinder, machine, and brewer service event types, multiple affected equipment records, photos, visible validation errors, edit/delete, Gear overview logging entry point, equipment detail history, and dashboard activity.
- Private media: app photos are served through `MediaAttachmentsController`, with active-workspace checks, contained private thumbnails, bounded no-crop `hero` variants (`resize_to_limit [1200, 1200]`), safe-raster gates, in-page lightbox viewing, view/download/crop/primary/remove controls, visible-image crop coordinate handling, and related photo groups, including private recipe finished-drink photos.
- Active-workspace export: owner-only structured JSON, beans CSV, brews CSV, and media ZIP with manifest for workspace-owned media. Brew JSON and CSV preserve the exact ordered recipient/Cup fields `recipient_kind`, `recipient_user_id`, `recipient_user_display_name`, `recipient_user_email_address`, `recipient_name`, and `cup_style`; new exports omit the two legacy Guest columns. JSON and Beans CSV preserve both direct private Bean websites. Curated public Bean, Brew, and Recipe snapshots exclude those fields, and public Brew rendering no longer exposes Bean purchase cost.
- Instance backups: instance-admin-only backup profiles can be activated/configured in the app, scheduled through Solid Queue, run manually, tracked with run history/error metadata, retained by profile policy, written as either full media ZIP archives or readable all-households JSON, validated, and restored into an empty server through Rails tasks with ID remapping and media integrity checks. Version-1 restore accepts both the new recipient schema and narrowly validated legacy Guest rows, maps household recipients only through archived User IDs, preserves former-member relations and Cup values, and rolls back malformed imports with a sanitized error. Readable and full backups preserve the private Bean Purchase Website and Coffee Origin Website after HTTP/HTTPS-with-host validation, while older archives without `coffee_origin_url` remain valid.
- Instance admin operations: read-only backup coverage/status, SMTP status, recent mail delivery failures, Solid Queue job counts, failed-job/failure surfacing, worker visibility, and workspace export availability with redacted operational errors.
- Beanconqueror import: conservative espresso-compatible JSON subset, raw import preservation, supported bean/equipment/preparation/brew metadata mapping, source UUID duplicate handling, warnings, and import reports.
- Analytics: workspace statistics with relative/manual/all-time ranges plus independent **Logged by** and **Served to** filters, all-time bean detail analytics, equipment detail analytics, and preparation tool detail analytics, all workspace scoped and query backed. Safe current and historical User labels are available without emails, named recipients combine their Self Brews with household-member servings, and Guests remain one unnamed aggregate. Date and people scopes apply to every Brew-derived total, cost, leader, rate, distribution, and series; current open Bean count, known Bean spend, and roaster/origin/process catalog facts stay unfiltered. Empty Brew scopes do not invent leaders or percentages, and Quick-Drip-only scopes have no Espresso channeling denominator. Bean open age now stops on the correct finished, archived, or latest-brew used-up date, and private bean Average Rating and Channeling cards show the same privacy-safe workspace rank badges as public bean shares. Quick Drip counts in broad brew/consumption totals and method distributions; channeling, retention, and grinder tendency stay espresso-only.
- Production self-hosting guide: deployment assumptions, environment/secrets handling, SMTP sender configuration, storage volume guidance, Ansible-friendly Compose/env examples, digest-based image upgrades, backup validation, empty-server restore drills, and upgrade checks.
- Presentation and setup polish: self-hosted Elms Sans, Roastnode brand assets, icon-only mobile-friendly back links that avoid share workflow loops, Material-symbol detail action buttons with mobile More menus for dense action sets, shared rounded photo upload controls that preserve existing photos on empty edit saves, a compact dashboard live time-since-last-coffee header that includes Self and household-member Brews but excludes only Guest Brews, row-major mixed coffee metric cards whose six comparison cards use locally vendored, lazily loaded Chart.js line/area backgrounds, split unopened-stock/open-bean inventory cards with open-bean and closed-bag counts, rough daily/weekly spend cards, dashboard stock shelf with quick-open/Rebuy affordances and bean cost metrics, a dashboard open bean cockpit with open/roast age, remaining inventory pressure, latest coffee setup, and best-brew links, Gear-centered equipment/tool return paths, a compact global footer with a left-aligned GitHub badge plus right-aligned app version or configured Buy Me a Coffee support badge, cross-links between domain records, optional demo data, setup-installed pre-commit RuboCop hooks, and documentation for each shipped slice.

## Brew Recipient And Hero Verification — 2026-08-23

- Task 2's independent frontend evaluation passed the shared Espresso, Quick Drip, and saved-Brew Served-to controls at desktop and `390×844`, with no clipping or horizontal overflow. Its report left a multi-member/long-label fixture and a real-keyboard focus-ring pass as follow-up checks rather than claiming those cases were evaluated.
- Task 3 reproduced the private Hero long-recipient overflow at `390px` and added structural regressions for both Hero variants and the independent localized Cup badge. Its protected browser re-pass was unavailable, so this ledger does not claim a separate Task 3 visual PASS.
- Task 6's independent public Brew evaluation passed both-image, Bean-only, Brew-only, neither-image, and malformed-recipient states at widths `1440`, `768`, and `375`, including black halves, contain/cover fit, readable overlays, opaque media, paired avatars, boundaries, and overflow.
- Task 7's independent public Bean evaluation passed Self, current household member, named/unnamed Guest, former member, forged malformed recipient, missing-image, and clustered-journey states at `1440×1000`, `768×900`, and `375×812`, with clean wrapping, opaque media, no private identity leakage, and no console errors.
- Task 8's final focused export/restore/privacy verification passed **50 runs / 720 assertions** with no failures or errors. This status update does not claim a fresh Task 9 browser pass or the later full-regression Task 10 gate.

## Changed From The Initial Idea

- Recipes were narrowed from the original broad v1 idea. The current implementation starts with exact-target recipe profiles, guided logging overlays, Hero Card ghost comparison, JSON portability, and curated public recipe pages. Machine-readable profile files remain a separate follow-up slice.
- Backups were originally framed as documentation plus workspace export. The current v1 direction now needs an in-app, instance-admin-only backup system with scheduled jobs, full reconstructable archives, and readable all-households JSON.
- Last-brew defaults were narrowed after product testing. The current contract copies only setup fields: bean, grinder, machine, preparation tools, grind setting, temperature, and supported pre-infusion/low-flow-start seconds.
- Beanconqueror compatibility means practical import first, not round-trip parity.
- Quick Drip v1 intentionally covers private automatic drip/filter-style daily logging. Quick Drip brews can appear as public-safe summaries inside public bean shares, but standalone public Quick Drip brew share pages, Quick Drip recipes, Beanconqueror Quick Drip import, brewer cup/water calibration, and professional filter methods remain deferred.
- Analytics calculations remain server-rendered/query-backed. The dashboard now uses Stimulus plus decorative Chart.js microcharts for six metric-card backgrounds, while richer interactive charts on the dedicated statistics page remain optional future work.
- Public identity images are now split by context: normal app media stays private behind authenticated routes, while workspace logos and user avatars referenced by enabled public brew, bean, or recipe share snapshots can render through the relevant public share media whitelist.

## Private Open Topics

Current roadmap and open-topic notes are intentionally kept out of the public repository.

## Source Files

- Original product context: private planning notes outside this repository.
- Repository-wide agent rules: `AGENTS.md`
- Backup system note: `docs/backup-system.md`
- Production self-hosting guide: `docs/production-self-hosting.md`
- Documentation index: `docs/README.md`
- Public brew sharing contract: `docs/public-brew-sharing.md`
- Public bean sharing contract: `docs/public-bean-sharing.md`
- Slice specs and implementation plans: `docs/superpowers/`
