<style>
  .brew-rating-bean {
    display: inline-block;
    width: clamp(.42rem, 1.7vw, .72rem);
    height: clamp(.58rem, 2.35vw, 1rem);
    border-radius: 60% 45% 60% 45%;
    transform: rotate(24deg);
  }

  .brew-metric-value {
    font-size: clamp(.68rem, 2.6vw, 1.125rem);
    line-height: 1.1;
    white-space: nowrap;
  }

  .brew-metric-subvalue {
    font-size: clamp(.5rem, 1.85vw, .72rem);
    line-height: 1;
    white-space: nowrap;
  }

  .brew-metric-target {
    font-size: clamp(.46rem, 1.45vw, .62rem);
    line-height: 1;
    white-space: nowrap;
  }

  .brew-chart-grid {
    background-image: radial-gradient(circle, #d6d3d1 1.2px, transparent 1.2px);
    background-size: 26px 26px;
  }

  @media (max-width: 639px) {
    .brew-chart-grid {
      background-size: 22px 22px;
    }
  }
</style>
